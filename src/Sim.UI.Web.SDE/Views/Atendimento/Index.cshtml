@model Sim.UI.Web.SDE.ViewModels.VMAtendimentoIndex

@{
    ViewData["Title"] = "Index";
}

<h5>Lista de Atendimentos</h5>

<partial name="_StatusMessage" model="Model.StatusMessage" />

<hr />
<pre>Data do Atendimento</pre>

<div class="form-row mb-3 align-content-center">
    <div class="form-group col-md-3">
        <form asp-action="Index">
            <div class="form-row">
                <div class="form-group">
                    <div class="input-group mb-3">
                        <button class="btn btn-outline-primary" type="submit" role="button" id="button-addon1"><i class="fas fa-chevron-left"></i></button>
                        <input asp-for="DataAtendimento" class="form-control" placeholder="Nome" aria-describedby="button-addon2">
                        <button class="btn btn-outline-primary" type="submit" role="button" id="button-addon2"><i class="fas fa-chevron-right"></i></button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<table class="table table-borderless bg-white">
    <caption>Lista de atendimentos!</caption>
    <thead>
        <tr class="border-bottom shadow-sm">
            <th>
                Contador
            </th>
            <th>
                Protocolo
            </th>
            <th>
                Horário
            </th>
            <th>
                Cliente
            </th>
            <th>
                Email
            </th>
            <th>
                Setor
            </th>
            <th>
                Canal
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ListaAtendimento)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Servicos)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Protocolo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Inicio)
                    -
                    @Html.DisplayFor(modelItem => item.Fim)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pessoa_Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Servicos)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Setor)
                <td>
                    @Html.DisplayFor(modelItem => item.Canal)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
